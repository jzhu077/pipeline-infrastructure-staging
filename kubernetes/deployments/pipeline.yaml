apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: pipeline
  name: pipeline
  namespace: default
spec:
  selector:
    matchLabels:
      app: pipeline
  template:
    metadata:
      labels:
        app: pipeline
    spec:
      containers:
      - image: gcr.io/platform-161007/pipeline:d812376e19106fc61c962eebe70229d0b7922f7e
        name: pipeline
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
